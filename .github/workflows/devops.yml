name: Python microservice test with Github Actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@master
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
    with:
      python-version: 3.9
    - name: Install dependencies
      run: |
       make install
    - name: Lint code
      run: |
       #make lint
   - name: Test microservice
     run: |
      #make test
   - name: Format code
     run: |
      #make format  
